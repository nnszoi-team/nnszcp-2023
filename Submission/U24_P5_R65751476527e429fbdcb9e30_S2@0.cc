#include<bits/stdc++.h>
using namespace std;
int ai[100000], bi[100000], ci[100000], di[100000], fi[100000], gi[100000];
int main(){
	int la, lb, lc, ld, lf, ls;
	int n, m;
	cin>>la>>lb>>lc>>ld>>lf>>ls;
	cin>>n>>m;
	for(int i=1;i<=n;++i){
		cin>>ai[i]>>bi[i]>>ci[i]>>di[i]>>fi[i]>>gi[i];
	}
//	for()
	return 0;
}